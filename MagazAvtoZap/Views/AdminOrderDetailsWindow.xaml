<Window x:Class="MagazAvtoZap.Views.AdminOrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали заказа" Height="500" Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock x:Name="OrderNumberTextBlock" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="OrderDateTextBlock" Margin="5"/>
            <TextBlock x:Name="StatusTextBlock" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock x:Name="FullNameTextBlock" Margin="5"/>
            <TextBlock x:Name="PhoneTextBlock" Margin="5"/>
            <TextBlock x:Name="AddressTextBlock" Margin="5"/>
            <TextBlock x:Name="PaymentMethodTextBlock" Margin="5"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="OrderItemsDataGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название товара" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalPrice, StringFormat='{}{0:C}'}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Изменить статус" Click="ChangeStatusButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>

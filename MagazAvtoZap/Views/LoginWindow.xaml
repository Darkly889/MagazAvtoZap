<Window x:Class="MagazAvtoZap.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="0" Grid.Column="1" Margin="10" VerticalAlignment="Center"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="10" VerticalAlignment="Center"/>
        <TextBox x:Name="ShowPasswordTextBox" Grid.Row="1" Grid.Column="1" Margin="10" VerticalAlignment="Center" Visibility="Collapsed" TextChanged="ShowPasswordTextBox_TextChanged"/>

        <CheckBox x:Name="ShowPasswordCheckBox" Content="Показать пароль" Grid.Row="2" Grid.ColumnSpan="2" Margin="10" VerticalAlignment="Center" Checked="ShowPasswordCheckBox_Checked" Unchecked="ShowPasswordCheckBox_Unchecked"/>

        <Button Content="Войти" Grid.Row="3" Grid.ColumnSpan="2" Margin="10" Padding="10,5" HorizontalAlignment="Center" Click="LoginButton_Click"/>
    </Grid>
</Window>

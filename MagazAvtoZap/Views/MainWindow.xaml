<Window x:Class="MagazAvtoZap.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Магазин автозапчастей" Height="600" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFF5E6" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#F0F0F0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>


            <Button Content="Обновить товары" Grid.Column="0" HorizontalAlignment="Left" Margin="10" Click="RefreshProductsButton_Click"/>


            <Button Content="Выйти" Grid.Column="1" HorizontalAlignment="Right" Margin="10" Click="LogoutButton_Click"/>


            <Button x:Name="AdminButton" Content="Администрирование" Grid.Column="1" HorizontalAlignment="Right" Margin="10,10,80,10" Click="AdminButton_Click" Visibility="Collapsed"/>
        </Grid>


        <DataGrid x:Name="ProductsDataGrid" AutoGenerateColumns="False" Grid.Row="1" Margin="10,10,10,10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="Auto"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="Auto"/>
                <DataGridTextColumn Header="Поставщик" Binding="{Binding Supplier}" Width="Auto"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Остаток" Binding="{Binding StockQuantity}" Width="Auto"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>


        <Grid Grid.Row="1" Margin="0,451,10,0" Height="85" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,13,192,7">
                <TextBlock Text="Количество:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="QuantityTextBox" Width="50" VerticalAlignment="Center" Margin="5" Text="1"/>
                <Button Content="Добавить в корзину" Click="AddToCartButton_Click" Width="120" Margin="5"/>
            </StackPanel>


            <Border Grid.Column="1" Width="50" Height="50" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Background="Transparent" Cursor="Hand" MouseLeftButtonDown="CartImage_MouseLeftButtonDown">
                <Grid Margin="-35,0,35,0">
                    <Image Source="/Images/cart.png" Width="94" Height="50" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-7,0,0,0"/>

                    <Border Background="Red" Width="20" Height="20" CornerRadius="10" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,-5,0,0">
                        <TextBlock Name="CartItemCountTextBlock" Text="0" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
